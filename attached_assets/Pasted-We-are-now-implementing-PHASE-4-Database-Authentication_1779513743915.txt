We are now implementing PHASE 4 (Database + Authentication) and PHASE 5 (Detection Engine) END-TO-END for the AI Phishing Detection Platform.

IMPORTANT:
These two phases MUST be completed with:

* production-level architecture
* no TypeScript errors
* no broken imports
* no API failures
* no authentication bugs
* no deployment issues
* no database schema conflicts
* no middleware failures
* no routing issues
* no placeholder implementations

Generate REAL WORKING CODE only.

The application must work correctly in:

* Replit
* Local development
* Vercel production deployment

The project MUST successfully run:

npm install
npm run dev
npm run build

WITHOUT:

* build errors
* hydration issues
* middleware issues
* Supabase session issues
* JWT validation failures
* 404 refresh problems

---

## PHASE 4 — DATABASE + AUTHENTICATION

---

## STEP 1 — SUPABASE DATABASE SETUP

Use Supabase PostgreSQL.

Generate complete database setup.

Create SQL schema for:

---

## TABLE: users

Fields:

* id (uuid primary key)
* name
* email (unique)
* role
* created_at

---

## TABLE: scan_history

Fields:

* id (uuid primary key)
* user_id (foreign key)
* scan_type
* content
* risk_score
* result
* created_at

---

## TABLE: reports

Fields:

* id (uuid primary key)
* report_type
* details
* created_at

---

## DATABASE REQUIREMENTS

Implement:

* indexes
* optimized queries
* foreign keys
* relationships
* cascade delete
* timestamps
* Row Level Security (RLS)
* secure access policies

Generate:

1. schema.sql
2. migration.sql
3. seed.sql

---

## STEP 2 — SUPABASE AUTHENTICATION

Implement complete authentication system using Supabase Auth.

Features:

* user registration
* login
* logout
* session persistence
* JWT validation
* protected sessions
* refresh token handling
* role-based access
* admin authorization

Generate:

* Supabase client
* Admin client
* Auth helpers
* Session utilities
* Token validation middleware

Environment variables:

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
JWT_SECRET=

---

## STEP 3 — AUTHENTICATION APIs

Create complete working APIs:

POST /api/auth/register
POST /api/auth/login
POST /api/auth/logout
GET /api/auth/profile

Features:

* password hashing
* JWT session handling
* duplicate email prevention
* email validation
* password strength validation
* secure cookies
* auth middleware

Generate:

* authController.ts
* authRoutes.ts
* authMiddleware.ts
* authValidators.ts
* tokenHelper.ts

---

## STEP 4 — AUTHORIZATION SYSTEM

Implement:

* role-based authentication
* admin middleware
* protected routes
* session verification
* secure API access

Roles:

* user
* admin

Protect:

* dashboard APIs
* scan history APIs
* admin APIs

---

## STEP 5 — SESSION MANAGEMENT

Implement:

* session persistence
* refresh token validation
* automatic token refresh
* secure logout
* session expiry handling

Ensure:

* sessions survive refresh
* middleware works correctly
* no redirect loops
* production-safe auth handling

---

## STEP 6 — FRONTEND AUTH INTEGRATION

Connect frontend authentication fully.

Features:

* login form
* register form
* protected routing
* auth redirects
* persistent login
* logout handling

Use:

* Zustand auth store
* Supabase session listener
* middleware.ts

---

## STEP 7 — MIDDLEWARE IMPLEMENTATION

Create reusable middleware:

1. authMiddleware.ts
2. adminMiddleware.ts
3. validationMiddleware.ts
4. errorMiddleware.ts

Implement:

* JWT validation
* session verification
* route protection
* centralized error handling

---

## STEP 8 — ERROR HANDLING SYSTEM

Create centralized error system.

Handle:

* invalid tokens
* expired sessions
* invalid credentials
* duplicate emails
* Supabase failures
* database errors
* unauthorized access

Return consistent API responses.

---

## PHASE 5 — DETECTION ENGINE

IMPORTANT:
This phase is the CORE of the project.

Implement a REAL phishing detection engine.

---

## STEP 9 — URL DETECTION ENGINE

Create complete phishing URL analysis system.

Create:

POST /api/scan/url

Implement checks:

1. HTTPS validation
2. Suspicious keyword detection
3. Long URL detection
4. Shortened URL detection
5. IP-based URL detection
6. Suspicious subdomain detection
7. Domain reputation analysis
8. Blacklisted domain detection
9. Redirect pattern analysis
10. Unicode spoofing detection
11. Homograph attack detection

Generate:

* urlAnalysisService.ts
* riskCalculator.ts
* domainUtils.ts

---

## STEP 10 — VIRUSTOTAL INTEGRATION

Integrate VirusTotal API fully.

Implement:

* URL reputation lookup
* malicious URL detection
* API abstraction layer
* timeout handling
* retry handling
* fallback handling
* clean response formatting

Environment variable:

VIRUSTOTAL_API_KEY=

Generate:

* virusTotalService.ts

---

## STEP 11 — GOOGLE SAFE BROWSING INTEGRATION

Integrate Google Safe Browsing API.

Implement:

* malicious URL detection
* phishing threat checks
* threat categorization
* reusable utilities

Environment variable:

GOOGLE_SAFE_BROWSING_API_KEY=

Generate:

* safeBrowsingService.ts

---

## STEP 12 — EMAIL DETECTION ENGINE

Create:

POST /api/scan/email

Implement phishing email analysis.

Detect:

* urgency language
* fake warnings
* credential requests
* banking fraud indicators
* scam keywords
* suspicious links
* impersonation attempts
* fear-based language
* financial fraud phrases

Generate:

* emailAnalysisService.ts
* phishingKeywordEngine.ts

Return:

* risk score
* detected indicators
* suspicious keywords
* safety recommendations

---

## STEP 13 — RISK SCORING SYSTEM

Implement intelligent risk scoring.

Risk Levels:
0-30 SAFE
31-60 SUSPICIOUS
61-100 HIGH RISK

Scoring Factors:

* suspicious keywords
* URL reputation
* domain structure
* malicious redirects
* phishing patterns
* API threat results

Generate:

* riskScoreEngine.ts

---

## STEP 14 — SCAN HISTORY STORAGE

Store all scan results securely.

Save:

* user_id
* scan_type
* content
* risk_score
* result
* created_at

Create:
GET /api/scan/history

Features:

* pagination
* filtering
* sorting
* authenticated access

---

## STEP 15 — DETECTION RESULT RESPONSE SYSTEM

Return detailed analysis results.

Example Response:

{
"riskScore": 82,
"status": "HIGH_RISK",
"threats": [
"Suspicious domain",
"Phishing keywords detected",
"Unsafe reputation"
],
"recommendation": "Avoid visiting this URL"
}

---

## STEP 16 — VALIDATION SYSTEM

Use express-validator.

Validate:

* URLs
* email content
* auth requests
* admin requests

Prevent:

* invalid URLs
* malicious payloads
* injection attempts

---

## STEP 17 — SECURITY IMPLEMENTATION

Implement:

* Helmet
* CORS
* Rate limiting
* input sanitization
* SQL injection prevention
* XSS prevention
* secure headers
* secure JWT handling

---

## STEP 18 — LOGGING SYSTEM

Configure:

* Morgan logging
* request logs
* error logs
* API monitoring logs

Create:
logs/

* error.log
* combined.log

---

## STEP 19 — FRONTEND ↔ BACKEND CONNECTION

Connect frontend pages fully.

Connect:

* login/register
* URL scanner
* email analyzer
* scan history
* dashboard analytics

Implement:

* loading states
* toast notifications
* API error handling
* retry handling

---

## STEP 20 — VERCEL + REPLIT COMPATIBILITY

VERY IMPORTANT:

Prevent:

* 404 refresh errors
* middleware deployment failures
* broken API routes
* session failures
* auth redirect loops

Generate:

1. vercel.json
2. production-safe middleware
3. deployment-safe auth handling

Use:

{
"rewrites": [
{
"source": "/(.*)",
"destination": "/"
}
]
}

Ensure:

* App Router works
* protected routes work
* middleware works in production
* refresh works correctly
* APIs work after deployment

---

## STEP 21 — BUILD VALIDATION

Ensure:

* npm install works
* npm run dev works
* npm run build works

Automatically fix:

* TypeScript errors
* import issues
* middleware issues
* hydration issues
* build failures

---

## STEP 22 — OUTPUT REQUIREMENTS

Generate:

1. Full authentication system
2. Full database implementation
3. Full detection engine
4. VirusTotal integration
5. Google Safe Browsing integration
6. Middleware system
7. Validation system
8. Risk scoring engine
9. Frontend-backend integration
10. Deployment-safe configuration
11. Supabase setup
12. Error handling system
13. Logging system

IMPORTANT:

* Generate clean production-level code
* Ensure scalable architecture
* Ensure reusable utilities
* Ensure secure implementation
* Ensure no placeholder code remains
* Ensure no unfinished APIs remain
* Ensure no broken imports remain
* Ensure the project is production-ready
* Ensure the project is interview-ready
* Ensure the project is portfolio-quality

These two phases must be completed FULLY end-to-end without bugs or errors.
