We are now starting PHASE 3 — BACKEND DEVELOPMENT for the AI Phishing Detection Platform.

IMPORTANT:
This phase must be implemented END-TO-END with production-level backend architecture.

The backend must be:

* scalable
* secure
* modular
* deployment-ready
* Vercel-compatible
* Supabase-compatible
* production-structured

The goal is to COMPLETE the backend implementation almost fully (75–85% production-ready).

DO NOT generate placeholder backend code.
DO NOT leave incomplete APIs.
DO NOT generate pseudo code.
Generate real working implementation.

---

## TECH STACK

Backend:

* Node.js
* Express.js
* TypeScript
* Supabase
* JWT Authentication
* bcrypt
* Helmet
* CORS
* dotenv
* Morgan
* Express Validator
* Rate Limiter

External APIs:

* VirusTotal API
* Google Safe Browsing API

---

## BACKEND FOLDER STRUCTURE

Generate complete backend structure:

backend/
├── controllers/
├── routes/
├── middleware/
├── services/
├── validators/
├── config/
├── utils/
├── database/
├── supabase/
├── types/
├── logs/
├── constants/
├── helpers/
├── app.ts
├── server.ts
└── package.json

Ensure clean scalable architecture.

---

## STEP 1 — INITIALIZE BACKEND

1. Setup Express server
2. Configure TypeScript
3. Configure tsconfig.json
4. Configure environment variables
5. Configure nodemon
6. Configure scripts
7. Configure logging
8. Configure error handling

Generate:

* package.json
* tsconfig.json
* nodemon.json
* .env.example

---

## STEP 2 — EXPRESS SERVER SETUP

Create:

* app.ts
* server.ts

Implement:

* Express initialization
* Helmet security
* CORS
* Morgan logging
* JSON parsing
* Rate limiting
* Global error middleware
* Route handling
* 404 handling
* Request validation middleware

Ensure production-level setup.

---

## STEP 3 — SUPABASE SETUP

Create:
backend/supabase/

Implement:

1. Supabase client
2. Admin client
3. Auth helpers
4. JWT validation
5. Session management

Use environment variables:

SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
JWT_SECRET=

Generate reusable Supabase utilities.

---

## STEP 4 — AUTHENTICATION SYSTEM

Implement complete authentication system.

Create APIs:

POST /api/auth/register
POST /api/auth/login
GET /api/auth/profile

Features:

* User registration
* Login
* Password hashing
* JWT token generation
* Session validation
* Role-based access
* Protected middleware
* Logout handling
* Token verification

Create:

* authController.ts
* authRoutes.ts
* authMiddleware.ts
* authValidators.ts

Validation:

* Email validation
* Password strength validation
* Duplicate user prevention

Error handling:

* Invalid credentials
* Expired token
* Unauthorized access

---

## STEP 5 — DATABASE IMPLEMENTATION

Use Supabase PostgreSQL.

Create SQL schema for:

1. users

* id
* name
* email
* role
* created_at

2. scan_history

* id
* user_id
* scan_type
* content
* risk_score
* result
* created_at

3. reports

* id
* report_type
* details
* created_at

Implement:

* Relationships
* Indexes
* RLS policies
* Secure queries
* Optimized inserts

Generate:

* migration.sql
* seed.sql

---

## STEP 6 — URL SCANNING API

Create:

POST /api/scan/url

Implement complete phishing URL detection.

Detection checks:

1. HTTPS check
2. Suspicious keywords
3. Long URL detection
4. IP-based URL detection
5. Domain reputation
6. Shortened URLs
7. Blacklisted domains
8. Suspicious subdomains

Generate:

* scanController.ts
* scanRoutes.ts
* urlAnalysisService.ts

Risk scoring:
0-30 SAFE
31-60 SUSPICIOUS
61-100 HIGH RISK

Store results in database.

---

## STEP 7 — EMAIL ANALYSIS API

Create:

POST /api/scan/email

Implement phishing email detection.

Detect:

* Urgency language
* Credential requests
* Scam keywords
* Fake warnings
* Financial fraud keywords
* Suspicious links

Generate:

* emailAnalysisService.ts
* emailScanner.ts

Return:

* risk score
* suspicious indicators
* safety recommendation

Store scan history.

---

## STEP 8 — VIRUSTOTAL INTEGRATION

Integrate VirusTotal API.

Implement:

* URL reputation lookup
* Error handling
* Timeout handling
* API abstraction layer
* Reusable service functions

Create:

* virusTotalService.ts

Environment variable:
VIRUSTOTAL_API_KEY=

Ensure:

* clean API response formatting
* retry handling
* fallback handling

---

## STEP 9 — GOOGLE SAFE BROWSING INTEGRATION

Integrate Google Safe Browsing API.

Create:

* safeBrowsingService.ts

Implement:

* malicious URL checks
* threat detection
* API abstraction
* reusable utilities

Environment variable:
GOOGLE_SAFE_BROWSING_API_KEY=

---

## STEP 10 — SCAN HISTORY APIs

Create:

GET /api/scan/history

Features:

* authenticated access
* pagination
* filtering
* sorting
* recent scans

Implement:

* historyController.ts
* pagination utilities

---

## STEP 11 — ADMIN APIs

Create:

GET /api/admin/users
GET /api/admin/reports
GET /api/admin/analytics

Admin features:

* user monitoring
* scan analytics
* phishing statistics
* suspicious activity logs

Add:

* admin middleware
* role validation

---

## STEP 12 — MIDDLEWARE SYSTEM

Create reusable middleware:

1. authMiddleware.ts
2. errorMiddleware.ts
3. validationMiddleware.ts
4. adminMiddleware.ts
5. rateLimiter.ts

Implement:

* token validation
* request validation
* centralized error handling
* role authorization

---

## STEP 13 — VALIDATION SYSTEM

Use express-validator.

Validate:

* URLs
* email content
* auth forms
* admin requests

Generate reusable validators.

---

## STEP 14 — SECURITY IMPLEMENTATION

Implement:

* Helmet
* CORS
* Rate limiting
* Input sanitization
* SQL injection prevention
* XSS prevention
* Secure headers
* Secure environment handling

Ensure production-level security.

---

## STEP 15 — LOGGING SYSTEM

Configure:

* Morgan logger
* request logs
* error logs

Create:
logs/

* error.log
* combined.log

---

## STEP 16 — UTILITIES & HELPERS

Create reusable utilities:

utils/

* apiResponse.ts
* asyncHandler.ts
* riskCalculator.ts
* validators.ts

helpers/

* tokenHelper.ts
* loggerHelper.ts

---

## STEP 17 — ERROR HANDLING

Create centralized error system.

Handle:

* validation errors
* auth errors
* API failures
* Supabase failures
* external API failures
* database failures

Return consistent JSON responses.

---

## STEP 18 — API DOCUMENTATION

Generate:

* API documentation
* request examples
* response examples
* authentication flow
* error response documentation

---

## STEP 19 — TESTING SETUP

Setup:

* API testing structure
* sample requests
* Postman collection structure

Ensure APIs work correctly.

---

## STEP 20 — VERCEL + REPLIT COMPATIBILITY

VERY IMPORTANT:

The backend MUST:

* deploy properly on Vercel
* work inside Replit
* avoid 404 issues
* avoid API route failures
* support environment variables correctly

Generate:

1. vercel.json
2. deployment configuration
3. production-safe routing
4. environment configuration

Use:

{
"rewrites": [
{
"source": "/(.*)",
"destination": "/"
}
]
}

Ensure:

* refresh works correctly
* APIs work after deployment
* no route conflicts
* no build failures

---

## OUTPUT REQUIREMENTS

Generate:

1. Full backend implementation
2. Complete folder structure
3. Working APIs
4. Middleware
5. Supabase integration
6. Detection engine
7. API integrations
8. Validation system
9. Security setup
10. Deployment setup
11. Logging system
12. Documentation
13. Environment examples

IMPORTANT:

* Ensure npm install works
* Ensure npm run dev works
* Ensure npm run build works
* Fix TypeScript issues automatically
* Ensure scalable clean architecture
* Ensure production-ready coding standards
* Ensure all imports are correct
* Ensure no placeholder APIs remain

The backend should be almost fully production-ready after this phase completes.
